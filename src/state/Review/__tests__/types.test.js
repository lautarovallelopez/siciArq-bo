import * as types from '../types';

describe('GET_SURVEYS_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.GET_SURVEYS_REQUEST).toBe(
            'Review/GET_SURVEYS_REQUEST'
        );
    });
});

describe('GET_SURVEYS_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.GET_SURVEYS_SUCCESS).toBe(
            'Review/GET_SURVEYS_SUCCESS'
        );
    });
});

describe('GET_SURVEYS_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.GET_SURVEYS_ERROR).toBe(
            'Review/GET_SURVEYS_ERROR'
        );
    });
});

describe('GET_SURVEY_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.GET_SURVEY_REQUEST).toBe(
            'Review/GET_SURVEY_REQUEST'
        );
    });
});

describe('GET_SURVEY_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.GET_SURVEY_SUCCESS).toBe(
            'Review/GET_SURVEY_SUCCESS'
        );
    });
});

describe('GET_SURVEY_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.GET_SURVEY_ERROR).toBe(
            'Review/GET_SURVEY_ERROR'
        );
    });
});

describe('FINISH_SURVEY_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.FINISH_SURVEY_REQUEST).toBe(
            'Review/FINISH_SURVEY_REQUEST'
        );
    });
});

describe('FINISH_SURVEY_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.FINISH_SURVEY_SUCCESS).toBe(
            'Review/FINISH_SURVEY_SUCCESS'
        );
    });
});

describe('FINISH_SURVEY_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.FINISH_SURVEY_ERROR).toBe(
            'Review/FINISH_SURVEY_ERROR'
        );
    });
});

describe('REASSIGN_SURVEY_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.REASSIGN_SURVEY_REQUEST).toBe(
            'Review/REASSIGN_SURVEY_REQUEST'
        );
    });
});

describe('REASSIGN_SURVEY_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.REASSIGN_SURVEY_SUCCESS).toBe(
            'Review/REASSIGN_SURVEY_SUCCESS'
        );
    });
});

describe('REASSIGN_SURVEY_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.REASSIGN_SURVEY_ERROR).toBe(
            'Review/REASSIGN_SURVEY_ERROR'
        );
    });
});

describe('APPROVE_SURVEY_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.APPROVE_SURVEY_REQUEST).toBe(
            'Review/APPROVE_SURVEY_REQUEST'
        );
    });
});

describe('APPROVE_SURVEY_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.APPROVE_SURVEY_SUCCESS).toBe(
            'Review/APPROVE_SURVEY_SUCCESS'
        );
    });
});

describe('APPROVE_SURVEY_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.APPROVE_SURVEY_ERROR).toBe(
            'Review/APPROVE_SURVEY_ERROR'
        );
    });
});

describe('REOPEN_SURVEY_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.REOPEN_SURVEY_REQUEST).toBe(
            'Review/REOPEN_SURVEY_REQUEST'
        );
    });
});

describe('REOPEN_SURVEY_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.REOPEN_SURVEY_SUCCESS).toBe(
            'Review/REOPEN_SURVEY_SUCCESS'
        );
    });
});

describe('REOPEN_SURVEY_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.REOPEN_SURVEY_ERROR).toBe(
            'Review/REOPEN_SURVEY_ERROR'
        );
    });
});

describe('SUPERVISE_SURVEY_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.SUPERVISE_SURVEY_REQUEST).toBe(
            'Review/SUPERVISE_SURVEY_REQUEST'
        );
    });
});

describe('SUPERVISE_SURVEY_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.SUPERVISE_SURVEY_SUCCESS).toBe(
            'Review/SUPERVISE_SURVEY_SUCCESS'
        );
    });
});

describe('SUPERVISE_SURVEY_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.SUPERVISE_SURVEY_ERROR).toBe(
            'Review/SUPERVISE_SURVEY_ERROR'
        );
    });
});

describe('RECOVER_SURVEY_REQUEST', () => {
    it('returns the correct action', () => {
        expect(types.RECOVER_SURVEY_REQUEST).toBe(
            'Review/RECOVER_SURVEY_REQUEST'
        );
    });
});

describe('RECOVER_SURVEY_SUCCESS', () => {
    it('returns the correct action', () => {
        expect(types.RECOVER_SURVEY_SUCCESS).toBe(
            'Review/RECOVER_SURVEY_SUCCESS'
        );
    });
});

describe('RECOVER_SURVEY_ERROR', () => {
    it('returns the correct action', () => {
        expect(types.RECOVER_SURVEY_ERROR).toBe(
            'Review/RECOVER_SURVEY_ERROR'
        );
    });
});
